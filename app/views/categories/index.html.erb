<h2>Categories List</h2>

<table>
  <thead>
    <th>Name</th> 
  </thead>
  <tbody>
    <% @categories.each do |f| %>
      <tr>
        <td><%= f.name %></td>
      </tr>
      <% end %>
  </tbody>
</table><br>
<%= link_to "New Category", new_category_path %>